<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="flyfish.mapper.PBL_ChatMessageMapper">


    <insert id="addclassPassword" useGeneratedKeys="true" keyProperty="id">
        insert into homework.user ( username, password, role_id)  values (#{username},#{password},#{roleId})
    </insert>
    <insert id="storePrivateMessage" useGeneratedKeys="true" keyProperty="messageId">
        insert into pbl.messages (sender_id, receiver_id, chat_type, message_type, message_content, sent_at,duration,fileSize,supplementary,call_Id,senderName,checkDate)
            values (#{senderId},#{receiverId},#{chatType},#{messageType},#{messageContent},#{sentAt},#{duration},#{fileSize},#{supplementary},#{callId},#{senderName},#{checkDate})
    </insert>
</mapper>