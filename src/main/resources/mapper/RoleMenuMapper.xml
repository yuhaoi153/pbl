<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="flyfish.mapper.RoleMenuMapper">

    <select id="getByRoleId" resultType="flyfish.pojo.RoleMenu">
        select * from homework.role_menu where role_id = #{roleId};

    </select>
</mapper>