<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="flyfish.mapper.M_StudentPerformLabelMapper">
    <insert id="insertBehaviorTag" parameterType="flyfish.pojo.M_BehaviorTag" useGeneratedKeys="true" keyProperty="id">
        insert into miniprograme.studentPerformLabel (school, educationType, wellBad, label)
        values (#{school}, #{educationType}, #{wellBad}, #{label})
    </insert>
</mapper>

